# Python TCP Port Scanner

A custom-built TCP port scanner written in Python.

## Usage
```bash
python3 scanner.py <target-ip> <start-port> <end-port>
```

## Features
- Multi-threaded scanning
- Identifies open ports on a target host
